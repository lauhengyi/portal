import Chart from "react-apexcharts";

export default function AnalyticsBar(analyticsResults: any, videoElement: any) {
    console.log(videoElement);
    return (
        <div className="analytics-bar">
            <h1>
                HELLO
            </h1>
        </div>
    );
}